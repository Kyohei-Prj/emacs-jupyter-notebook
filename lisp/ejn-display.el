;;; ejn-display.el --- ewoc-based notebook display layer  -*- lexical-binding: t; -*-

;; SPDX-License-Identifier: GPL-3.0-or-later

;; Commentary:

;; This module builds the read-only notebook display buffer using ewoc.
;; It renders cell headers, manages cell editing pop-ups, and provides
;; keyboard bindings for cell manipulation (insert, delete, move, execute).

;; Code:

(require 'ejn-util)
;; TODO: implementation

(provide 'ejn-display)
;;; ejn-display.el ends here
