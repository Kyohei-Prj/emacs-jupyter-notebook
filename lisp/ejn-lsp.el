;;; ejn-lsp.el --- LSP adapter and integration  -*- lexical-binding: t; -*-

;; SPDX-License-Identifier: GPL-3.0-or-later

;; Commentary:

;; This module integrates lsp-mode into cell buffers and the shadow buffer.
;; It handles LSP workspace root resolution, cell URI construction, single-cell
;; activation, cross-cell completion merging, and definition routing.

;; Code:

(require 'ejn-util)
;; TODO: implementation

(provide 'ejn-lsp)
;;; ejn-lsp.el ends here
